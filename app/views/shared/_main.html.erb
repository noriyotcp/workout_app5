<div class="row">
  <div class="col-md-12">
    <% flash.each do |key, message| %>
      <div class="text-danger alert
                  alert-<%= key == 'notice' ? 'success' : 'danger' %>">
        <%= message %>
      </div>
    <% end %>
  </div>
  <%= yield %>
</div>
